import { Routes } from '@angular/router';
import { PositionsComponent } from './positions.component';

export const POSITIONS_ROUTES: Routes = [
  { path: '', component: PositionsComponent, title: 'Postes' },
];
