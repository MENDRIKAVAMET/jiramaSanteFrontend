import { Routes } from '@angular/router';
import { DiagnosticsComponent } from './diagnostics.component';

export const DIAGNOSTICS_ROUTES: Routes = [
  { path: '', component: DiagnosticsComponent, title: 'Diagnostics' },
];
