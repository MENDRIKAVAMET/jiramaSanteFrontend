import { Routes } from '@angular/router';
import { DeclarationsComponent } from './declarations.component';

export const DECLARATIONS_ROUTES: Routes = [
  { path: '', component: DeclarationsComponent, title: 'Déclarations' },
];
