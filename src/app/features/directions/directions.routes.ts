import { Routes } from '@angular/router';
import { DirectionsComponent } from './directions.component';

export const DIRECTIONS_ROUTES: Routes = [
  { path: '', component: DirectionsComponent, title: 'Directions' },
];
