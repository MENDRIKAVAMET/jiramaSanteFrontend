import { Routes } from '@angular/router';
import { SymptomsComponent } from './symptoms.component';

export const SYMPTOMS_ROUTES: Routes = [
  { path: '', component: SymptomsComponent, title: 'Symptômes' },
];
